<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :immigrant_id %><br />
    <%= f.number_field :immigrant_id %>
  </div>
  <div class="field">
    <%= f.label :event_time %><br />
    <%= f.datetime_select :event_time %>
  </div>
  <div class="field">
    <%= f.label :event_place %><br />
    <%= f.text_field :event_place %>
  </div>
  <div class="field">
    <%= f.label :event_description %><br />
    <%= f.text_area :event_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
